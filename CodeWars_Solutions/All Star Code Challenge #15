#All Star Code Challenge #15

def rotate(str_):
    s = list(str_)
    test = []
    while len(test) != len(str_):
        for i in s:
            x = s[0]
            s.pop(0)
            s.append(x)
            y = ''.join(s)
            test.append(y)
    return test
