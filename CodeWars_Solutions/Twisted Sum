Twisted Sum

def compute_sum(n):
    ans = 0
    for num in range(n + 1):
        x = str(num)
        if len(x) > 1:
            for char in x:
                y = int(char)
                ans += y
        else:
            ans += num
    return ans
    
