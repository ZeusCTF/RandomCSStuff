Moving Zeros To The End
Python:

def move_zeros(array):
    for num in array:
        if num == 0:
            array.remove(num)
            array.append(0)
        else:
            pass
    return array
