def main(queryIP):

    def ipv4(IP):
        s = ''
        count = 0
        for byte in IP:
            



    if '.' in queryIP:
        ipv4(queryIP)
    if ':' in queryIP:
        ipv6(queryIP)


main('127.0.0.1')