Multiplication table
Python:

def multiplication_table(size):
    total = []
    num = 0
    for i in range(size + 1):
        i = []
        total.append(i)
    while num != size + 1:
        for i in range(size + 1):
            if i != 0:
                total[num].append(i * num)
        num += 1
    total.pop(0)
    return total
